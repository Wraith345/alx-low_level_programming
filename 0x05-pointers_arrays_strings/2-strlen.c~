#include <stdio.h>
/**
 * _strlen - check for length of string
 * @length: holds return value 
 * Return: length of string
 */
int _strlen(char *s)
{
  int length;

  length = sizeof(*s);
  return (length);
}
