#include <stdio.h>
/**
 * swap_int - swaps value of two integers
 * @*a: first integer
 * @*b: second integer
 * Return: 0
 */
void swap_int(int *a, int *b)
{
  int hold;

  hold = *a;
  *a = *b;
  *b = hold;
}
